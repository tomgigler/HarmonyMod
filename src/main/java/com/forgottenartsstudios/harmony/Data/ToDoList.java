package com.forgottenartsstudios.harmony.Data;

public class ToDoList {
    /*
    -   LastStartTime needs to be udpdated when the bot starts even if default params exist.
    -   Update player exists message to correct bound server from player info.

    * Example Code *
    jda.getGuildById(guild.getIdLong()).updateCommandPrivilegesById(936625908255703051L, CommandPermissions.enableCommandByUser(args[1])).queue();
     */
}
